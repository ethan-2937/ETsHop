<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.etshop.mappers.SysCategoryMapper">

	<!--实体映射-->
	<resultMap id="base_result_map" type="com.etshop.entity.po.SysCategory">
		<!---->
		<result column="category_id" property="categoryId"  />
		<!---->
		<result column="category_name" property="categoryName"  />
		<!---->
		<result column="p_category_id" property="pCategoryId"  />
		<!---->
		<result column="sort" property="sort"  />
		<collection property="productPropertyList" ofType="com.etshop.entity.po.SysProductProperty">
			<!--属性ID-->
			<result column="sp_property_id" property="propertyId"  />
			<!--属性名称-->
			<result column="sp_property_name" property="propertyName"  />
			<!--一级分类-->
			<result column="sp_p_category_id" property="pCategoryId"  />
			<!--二级分类-->
			<result column="sp_category_id" property="categoryId"  />
			<!--排序-->
			<result column="sp_property_sort" property="propertySort"  />
			<!--0:无需传封面 1:需传封面-->
			<result column="sp_cover_type" property="coverType"  />
		</collection>
	</resultMap>




	<!-- 通用查询结果列-->
	<sql id="base_column_list">
	 s.category_id,s.category_name,s.p_category_id,s.sort
	</sql>

	<sql id="base_condition_filed">
			<if test="query.categoryId != null and query.categoryId!=''">
				 and  s.category_id = #{query.categoryId}
			</if>
			<if test="query.categoryName != null and query.categoryName!=''">
				 and  s.category_name = #{query.categoryName}
			</if>
			<if test="query.pCategoryId != null and query.pCategoryId!=''">
				 and  s.p_category_id = #{query.pCategoryId}
			</if>
			<if test="query.sort != null">
				 and  s.sort = #{query.sort}
			</if>
	</sql>

	<!-- 通用查询条件列-->
	<sql id="query_condition">
	 <where>
			<include refid="base_condition_filed" />
			<if test="query.categoryIdFuzzy!= null  and query.categoryIdFuzzy!=''">
				 and  s.category_id like concat('%', #{query.categoryIdFuzzy}, '%')
			</if>
			<if test="query.categoryNameFuzzy!= null  and query.categoryNameFuzzy!=''">
				 and  s.category_name like concat('%', #{query.categoryNameFuzzy}, '%')
			</if>
			<if test="query.pCategoryIdFuzzy!= null  and query.pCategoryIdFuzzy!=''">
				 and  s.p_category_id like concat('%', #{query.pCategoryIdFuzzy}, '%')
			</if>
			<if test="query.categoryOrPCategory!= null  and query.categoryOrPCategory!=''">
				 and  s.category_id = #{query.categoryOrPCategory} or s.p_category_id = #{query.categoryOrPCategory}
			</if>
	 </where>
	</sql>

	<!-- 查询集合-->
	<select id="selectList" resultMap="base_result_map" >
		 SELECT <include refid="base_column_list" />
		     <if test="query.queryProperty">
		        ,sp.property_id sp_property_id,
		     sp.property_name sp_property_name,
		     sp.p_category_id sp_p_category_id,
		     sp.category_id sp_category_id,
		     sp.property_sort sp_property_sort,
		     sp.cover_type sp_cover_type
		     </if>
		     FROM sys_category s
		     <if test="query.queryProperty">
		        left join sys_product_property sp on  sp.category_id=s.category_id
		     </if>
		     <include refid="query_condition" />
		 <if test="query.orderBy!=null">
			 order by ${query.orderBy}
		 </if>
		 <if test="query.simplePage!=null">
			 limit #{query.simplePage.start},#{query.simplePage.end}
		 </if>
	</select>

	<!-- 查询数量-->
	<select id="selectCount" resultType="java.lang.Integer" >
		 SELECT count(1) FROM sys_category s <include refid="query_condition" />
	</select>

	<!-- 插入 （匹配有值的字段）-->
	<insert id="insert" parameterType="com.etshop.entity.po.SysCategory">
		 INSERT INTO sys_category
		 <trim prefix="(" suffix=")" suffixOverrides="," >
			<if test="bean.categoryId != null">
				 category_id,
			</if>
			<if test="bean.categoryName != null">
				 category_name,
			</if>
			<if test="bean.pCategoryId != null">
				 p_category_id,
			</if>
			<if test="bean.sort != null">
				 sort,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides="," >
			<if test="bean.categoryId!=null">
				 #{bean.categoryId},
			</if>
			<if test="bean.categoryName!=null">
				 #{bean.categoryName},
			</if>
			<if test="bean.pCategoryId!=null">
				 #{bean.pCategoryId},
			</if>
			<if test="bean.sort!=null">
				 #{bean.sort},
			</if>
		 </trim>
	</insert>

	<!-- 插入或者更新 （匹配有值的字段）-->
	<insert id="insertOrUpdate" parameterType="com.etshop.entity.po.SysCategory">
		 INSERT INTO sys_category
		 <trim prefix="(" suffix=")" suffixOverrides=",">
			<if test="bean.categoryId != null">
				 category_id,
			</if>
			<if test="bean.categoryName != null">
				 category_name,
			</if>
			<if test="bean.pCategoryId != null">
				 p_category_id,
			</if>
			<if test="bean.sort != null">
				 sort,
			</if>
		 </trim>
		 <trim prefix="values (" suffix=")" suffixOverrides=",">
			<if test="bean.categoryId!=null">
				 #{bean.categoryId},
			</if>
			<if test="bean.categoryName!=null">
				 #{bean.categoryName},
			</if>
			<if test="bean.pCategoryId!=null">
				 #{bean.pCategoryId},
			</if>
			<if test="bean.sort!=null">
				 #{bean.sort},
			</if>
		 </trim>
		 on DUPLICATE key update 
		 <trim prefix="" suffix="" suffixOverrides=",">
			<if test="bean.categoryId!=null">
				 category_id = VALUES(category_id),
			</if>
			<if test="bean.categoryName!=null">
				 category_name = VALUES(category_name),
			</if>
			<if test="bean.pCategoryId!=null">
				 p_category_id = VALUES(p_category_id),
			</if>
			<if test="bean.sort!=null">
				 sort = VALUES(sort),
			</if>
		 </trim>
	</insert>

	<!-- 添加 （批量插入）-->
	<insert id="insertBatch" parameterType="com.etshop.entity.po.SysCategory">
		 INSERT INTO sys_category(
			 category_id,
			 category_name,
			 p_category_id,
			 sort
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.categoryId},
				 #{item.categoryName},
				 #{item.pCategoryId},
				 #{item.sort}
			 )
		 </foreach>
	</insert>

	<!-- 批量新增修改 （批量插入）-->
	<insert id="insertOrUpdateBatch" parameterType="com.etshop.entity.po.SysCategory">
		 INSERT INTO sys_category(
			 category_id,
			 category_name,
			 p_category_id,
			 sort
		 )values
		 <foreach collection="list" item="item" separator=",">
			 (
				 #{item.categoryId},
				 #{item.categoryName},
				 #{item.pCategoryId},
				 #{item.sort}
			 )
		 </foreach>
			on DUPLICATE key update 
			category_id = VALUES(category_id),
			category_name = VALUES(category_name),
			p_category_id = VALUES(p_category_id),
			sort = VALUES(sort)
	</insert>

	<!--多条件修改-->
	<update id="updateByParam" parameterType="com.etshop.entity.query.SysCategoryQuery">
		 UPDATE sys_category s
 		 <set> 
			<if test="bean.categoryId != null">
				 category_id = #{bean.categoryId},
			</if>
			<if test="bean.categoryName != null">
				 category_name = #{bean.categoryName},
			</if>
			<if test="bean.pCategoryId != null">
				 p_category_id = #{bean.pCategoryId},
			</if>
			<if test="bean.sort != null">
				 sort = #{bean.sort},
			</if>
 		 </set>
 		 <include refid="query_condition" />
	</update>

	<!--多条件删除-->
	<delete id="deleteByParam">
		 delete s from sys_category s
 		 <include refid="query_condition" />
	</delete>

	<!-- 根据CategoryId修改-->
	<update id="updateByCategoryId" parameterType="com.etshop.entity.po.SysCategory">
		 UPDATE sys_category
 		 <set> 
			<if test="bean.categoryName != null">
				 category_name = #{bean.categoryName},
			</if>
			<if test="bean.pCategoryId != null">
				 p_category_id = #{bean.pCategoryId},
			</if>
			<if test="bean.sort != null">
				 sort = #{bean.sort},
			</if>
 		 </set>
 		 where category_id=#{categoryId}
	</update>

	<!-- 根据CategoryId删除-->
	<delete id="deleteByCategoryId">
		delete from sys_category where category_id=#{categoryId}
	</delete>

	<!-- 根据PrimaryKey获取对象-->
	<select id="selectByCategoryId" resultMap="base_result_map" >
		select <include refid="base_column_list" /> from sys_category s where category_id=#{categoryId}
	</select>

	<!-- 根据父CategoryId查询最大排序-->
	<select id="selectMaxSort" resultType="java.lang.Integer">
		select ifnull(MAX(sort),0) from sys_category where p_category_id=#{pCategoryId}
	</select>

	<!-- 批量更新排序-->
	<update id="updateSortBatch" parameterType="com.etshop.entity.po.SysCategory">
		<foreach collection="categoryList" item="item" separator=";">
			update sys_category set sort=#{item.sort} where category_id=#{item.categoryId}
		</foreach>
	</update>
	</mapper>
